#include"def.h"
#include"binary.h"

void solve(int testCase){
    long long a,b;
    cin>>a>>b;
    long long answer=0;
    vector<int> bin=getBinary(b);
    int len=bin.size();
    for(int i=0; i<len; i++){
        
    }

}