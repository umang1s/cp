#include<iostream>
#include"counter.h"
using namespace std;
/*      this is only for first
int main()
{
    counter<int> counter1;
    counter<short> counter2;
    int N;
    do
    {
        cout<<"enter number (for exit -1)";
        cin>>N;
        if(N !=-1)
        {
            if(N%2==0) counter1.increament();
            if(N%3==0) counter2.increament();
        }
    }while(N !=-1);
    cout << "even number entered : "<<counter1.getValue()<<endl<<"multiple of three is : "<<counter2.getValue()<<endl;
}
*/
int main()
{
    general_counter<int> difference;
    int N;
    do
    {
        cout <<"enter a number (
    }
}
