#include"default.h"

void first(){
    /*
    int arr[100];
    int n = sizeof(arr)/sizeof(arr[0])

    vector<int> v(arr,arr+n)

    //for changing the value of vector 
    for(auto *x: vector_arr) x=5 //this will change all elements to 5

    //we use rbegin to reverse.
    //we use rend to start.

    Function
        pop_back()	//remove last element
        front()		//return first element
        back()		//return last to arr
        insert(iterators,value)	//
        insert(terators,times,value)//insert times of value
        erase()     //erase the arr.
        clear()     //delete the arr.
        resize()    //
        empty()     //

    */
}

